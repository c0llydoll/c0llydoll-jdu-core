// TODO: handles all wdf stuff so that we dont have to update all funcs for a new feature

class WDF {
    constructor(version) {}

    async checkToken(token) {}
    async getPlayListPos() {}
    async sendScore() {}
}