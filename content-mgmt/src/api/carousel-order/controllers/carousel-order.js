'use strict';

/**
 * carousel-order controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::carousel-order.carousel-order');
